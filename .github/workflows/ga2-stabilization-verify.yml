name: GA-2 Production Stabilization

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ga2-stabilization-gate:
    name: GA-2 Legacy + Stabilization
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build marketplace contracts package
        run: npm run build --prefix packages/marketplace-contracts

      - name: GA-2 readiness verification
        run: npm run verify:ga2

      - name: Security test suite
        run: npm run test:security

      - name: Frontend production build
        run: npm run build:web

      - name: API production build
        run: npm run build:server
