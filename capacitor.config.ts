import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.manaintibojanam.app',
  appName: 'Mana Inti Bojanam',
  webDir: 'dist'
};

export default config;
