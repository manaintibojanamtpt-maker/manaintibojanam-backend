replicaCount: 1
image:
  repository: outbox-service
  tag: staging-latest
serviceAccount:
  name: staging-outbox-sa
resources:
  requests: { cpu: 250m, memory: 256Mi }
  limits: { cpu: 500m, memory: 512Mi }
autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 2
  targetOutboxDepth: 500
