# =============================================================================
# BhojanOS — Dev / Prod architecture (free-tier Spark safe)
# =============================================================================
#
# PRODUCTION (live customers)
#   Frontend : Vercel  → bhojanos.com
#   Backend  : Render  → manaintibojanam-backend.onrender.com
#   Firebase : bhojanos-prod (Firestore + Auth + Storage) — live customers
#   Pinger   : UptimeRobot/cron-job.org → GET /api/health every 10–14 min
#
# DEVELOPMENT (local + Vercel preview)
#   Frontend : localhost:8080 or Vercel Preview URL
#   Backend  : localhost:5000 (npm run dev) — do NOT point dev at prod Firestore for load tests
#   Firebase : create separate Spark project e.g. bhojanos-dev (own free quota)
#
# RETIRE: manaintibojanam Firebase for live traffic (legacy name only on Render URL)
# DO NOT: deploy same SPA to Vercel + Firebase Hosting (doubles Firestore reads)
#
# =============================================================================

# --- Platform tier ---
# free     = Spark-safe defaults (prod default on Render)
# standard = full AutoPilot crons + client telemetry (when on Blaze/paid)
PLATFORM_TIER=free
VITE_PLATFORM_TIER=free

# =============================================================================
# PRODUCTION — Render (manaintibojanam-backend)
# =============================================================================
NODE_ENV=production
PLATFORM_TIER=free
FIREBASE_PROJECT_ID=bhojanos-prod
FIREBASE_STORAGE_BUCKET=bhojanos-prod.firebasestorage.app
GOOGLE_CLOUD_PROJECT=bhojanos-prod
# Web SDK (also exposed via GET /api/client-config when Vercel env missing)
FIREBASE_WEB_API_KEY=
FIREBASE_WEB_APP_ID=
FIREBASE_WEB_MESSAGING_SENDER_ID=
FIREBASE_AUTH_DOMAIN=bhojanos-prod.firebaseapp.com
# Paste full JSON from Firebase → Project settings → Service accounts
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}

EMAIL_USER=bhojanos26@gmail.com
EMAIL_PASS=your_gmail_app_password
EMAIL_FROM=BhojanOS <bhojanos26@gmail.com>
FOUNDER_EMAIL=bhojanos26@gmail.com
FOUNDER_NAME=The BhojanOS Team

RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
CRON_SECRET=long_random_secret

WORKER_INTERVAL_MS=600000
SETTINGS_CACHE_MS=600000
TENANT_CACHE_MS=600000
FIRESTORE_QUOTA_BACKOFF_MS=900000
CRON_STARTUP_DELAY_MS=120000
STOREFRONT_BASE_URL=https://bhojanos.com
ORDERBHOJAN_URL=https://orderbhojan.web.app
ORDERBHOJAN_PUBLIC_URL=https://orderbhojan.web.app
PUBLIC_APP_URL=https://www.bhojanos.com
FOUNDER_STOREFRONT_URL=https://www.bhojanos.com/k/mana-inti

# =============================================================================
# PRODUCTION — Vercel (Environment: Production)
# =============================================================================
VITE_APP_ENV=production
VITE_PLATFORM_TIER=free
VITE_API_URL=https://manaintibojanam-backend.onrender.com
VITE_FIREBASE_PROJECT_ID=bhojanos-prod
VITE_FIREBASE_AUTH_DOMAIN=bhojanos-prod.firebaseapp.com
VITE_FIREBASE_STORAGE_BUCKET=bhojanos-prod.firebasestorage.app
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
VITE_RAZORPAY_KEY_ID=
VITE_ORDERBHOJAN_URL=https://orderbhojan.web.app
VITE_ORDERBHOJAN_PUBLIC_URL=https://orderbhojan.web.app

# =============================================================================
# DEVELOPMENT — local .env (copy to .env.local)
# =============================================================================
# VITE_APP_ENV=development
# VITE_PLATFORM_TIER=standard
# VITE_API_URL=http://localhost:5000
# VITE_FIREBASE_PROJECT_ID=bhojanos-dev
# VITE_FIREBASE_AUTH_DOMAIN=bhojanos-dev.firebaseapp.com
# VITE_FIREBASE_STORAGE_BUCKET=bhojanos-dev.firebasestorage.app
# GOOGLE_CLOUD_PROJECT=bhojanos-dev
# FIREBASE_PROJECT_ID=bhojanos-dev

# =============================================================================
# DEVELOPMENT — Vercel (Environment: Preview only)
# =============================================================================
# Same as local dev — use bhojanos-dev so preview branches never burn prod quota

VITE_GOOGLE_MAPS_API_KEY=

# M2 LocationSDK strangler flags (default off — no behaviour change until PR-4+)
# VITE_FF_LOCATION_MAP_ENABLED=false
# VITE_FF_LOCATION_DISCOVERY_ENABLED=false
# VITE_FF_LOCATION_OWNER_REGISTRATION_ENABLED=false
# VITE_FF_LOCATION_CUSTOMER_DETECTION_ENABLED=false

# M3 Discovery Intelligence (default OFF — zero production impact)
# VITE_FF_DISCOVERY_ENABLED=false
# VITE_FF_DISCOVERY_RANKING_ENABLED=false
# VITE_FF_DISCOVERY_MARKETPLACE_ENABLED=false
# VITE_FF_DISCOVERY_TENANT_REPOSITORY_ENABLED=false
# VITE_FF_DISCOVERY_ELIGIBILITY_ENABLED=false
# VITE_FF_DISCOVERY_GEOINDEX_ENABLED=false

# M4 Search Intelligence Platform (all OFF by default)
# VITE_FF_SEARCH_ENABLED=false
# VITE_FF_SEARCH_AUTOCOMPLETE_ENABLED=false
# VITE_FF_SEARCH_SUGGESTIONS_ENABLED=false
# VITE_FF_SEARCH_REPOSITORY_ENABLED=false

# M7 Menu & Catalog SDK (all OFF by default)
# VITE_FF_MENU_ENABLED=false
# VITE_FF_MENU_SEARCH_ENABLED=false
# VITE_FF_MENU_PROJECTION_ENABLED=false

# TinyFish menu search (Render backend secrets — NOT client-side)
# TINYFISH_API_KEY=
# TINYFISH_SEARCH_API_KEY=            # alias for TINYFISH_API_KEY
# TINYFISH_MENU_SEARCH_ENABLED=true   # set false to force Firestore-only menu search
# TINYFISH_SEARCH_LOCATION=IN
# TINYFISH_SEARCH_LANGUAGE=en
# TINYFISH_SEARCH_TIMEOUT_MS=800
# TINYFISH_SEARCH_CACHE_TTL_MS=300000

# Shared AI Platform / OpenRouter (Render secrets — NEVER VITE_*)
# Docs: docs/ai-platform/PHASE-1.md
# AI_GATEWAY_ENABLED=false            # must be true to accept /api/ai/v1/assist
# OPENROUTER_API_KEY=
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# OPENROUTER_MODEL=openai/gpt-oss-20b:free
# AI_GATEWAY_TIMEOUT_MS=15000
# AI_GATEWAY_MAX_TOKENS=1024
# AI_GATEWAY_RATE_LIMIT_MAX=60
# VITE_FF_AI_MARKETING_ASSISTANT=false
# Phase 9: AI metrics are always collected in-process when the gateway handles traffic;
# no separate enable flag. Query via GET /api/ai/v1/status (observability) or
# GET /api/ops/ai/summary (superadmin).
# Phase 11/13: canary policy — assist gate inactive unless WIRED + ENABLED + stage>0
# AI_CANARY_ROLLOUT_ENABLED=false
# AI_CANARY_ROLLOUT_STAGE=0
# AI_CANARY_WIRED_INTO_ASSIST=false  # bhojanos.com UI (Phase 7); OFF by default
# Phase 21: durable AI audit events → Firestore ai_audit_events (fire-and-forget)
# AI_AUDIT_PERSISTENCE_ENABLED=false
# Phase 23: offline golden eval harness (CI/pre-canary); npm run test:ai:golden always works
# AI_EVAL_HARNESS_ENABLED=false
# Phase 24: non-user-visible shadow capture from audit events + offline replay compare
# AI_SHADOW_TRAFFIC_ENABLED=false
# AI_SHADOW_TRAFFIC_SAMPLE_RATE=1
# AI_SHADOW_TRAFFIC_MAX_BATCH=50
# AI_SHADOW_PERSISTENCE_ENABLED=false
# Phase 25: advisory live canary rollout gates (promotion / halt / rollback) — never auto-promote
# AI_CANARY_LIVE_ROLLOUT_GATES_ENABLED=false
# AI_CANARY_MANUAL_APPROVAL_GRANTED=false
# AI_CANARY_ROLLOUT_STAGE_SET_AT=  # ISO timestamp when current stage was set (soak)
# AI_CANARY_ROLLOUT_MIN_SOAK_HOURS=24
# AI_CANARY_GOLDEN_MIN_PASS_RATE=1.0
# AI_CANARY_SHADOW_MIN_SAMPLES=10
# AI_CANARY_SHADOW_MAX_DRIFT_RATE=0.05
# AI_CANARY_MAX_ERROR_RATE_PERCENT=10
# AI_CANARY_GOLDEN_PRECHECK_PASSED=  # optional true|false override after CI golden run

# =============================================================================
# GA-1 PRODUCTION — Projection flags MUST remain OFF (legacy path only)
# =============================================================================
# Do NOT set any of these to true in Vercel Production or Render.
# Verification: npm run verify:ga1-flags
# Manifest: scripts/flags/ga1-production-flags.json
# Docs: docs/ga-1/PRODUCTION-FLAG-MANIFEST.md
#
# VITE_FF_EVENT_PLATFORM_ENABLED=false
# VITE_FF_ORDER_READ_PROJECTION_ENABLED=false
# VITE_FF_ORDER_PROJECTION_ADAPTER_ENABLED=false
# VITE_FF_MENU_PROJECTION_ENABLED=false
# VITE_FF_MENU_PROJECTION_ADAPTER_ENABLED=false
# VITE_FF_PRICING_PROJECTION_ENABLED=false
# VITE_FF_PRICING_PROJECTION_ADAPTER_ENABLED=false
# (see full list in ga1-production-flags.json)

WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
BIOMETRIC_SALT=
JWT_SECRET=
